{
  "name": "frontend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "npm run index.ts",
    "dev": "tsx watch index.ts",
    "build": "npm build index.ts --outdir dist"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}