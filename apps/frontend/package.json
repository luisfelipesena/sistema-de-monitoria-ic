{
  "name": "frontend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "npm run index.ts",
    "dev": "npm --watch run index.ts",
    "build": "npm build index.ts --outdir dist"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}